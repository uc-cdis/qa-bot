apiVersion: v1
kind: Service
metadata:
  name: anythingllm-service
spec:
  selector:
    app: anythingllm
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http